node_modules/
dist/

# Network responses and incremental-build bookkeeping.
.cache/

# Generated by `npm run content` — rebuild instead of committing.
public/data/
public/media/

# Raw illustration-generator output — the prompts that made them are the
# source material worth keeping; the images themselves are reprocessed into
# content/images/ before they're used.
content/illustration-prompts/generated-images/
